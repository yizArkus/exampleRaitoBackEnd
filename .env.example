# Puerto del API
PORT=3000

# JWT (usa un valor largo y aleatorio en producción)
JWT_SECRET=cambia-esto-en-produccion
JWT_EXPIRES_IN=8h

# CORS: el origen permitido está fijado en src/app.ts (solo AWS Amplify).
